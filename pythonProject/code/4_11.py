# 初始化一个空列表，用于存放结果
result = []

# 遍历1到1000之间的整数
for i in range(1, 1001):
    # 检查数字是否满足给定的除法余数条件
    if i % 3 == 2 and i % 5 == 3 and i % 7 == 2:
        result.append(i)  # 如果满足条件，将其添加到结果列表中

# 使用空格将结果列表中的数字连接成一个字符串，并打印
print(f"0~1000中用3除余2，用5除余3，用7除余2的数有：\n{' '.join(map(str, result))}")
